name: Generate languages card

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    permissions:
      contents: write

    steps:
      - name: Generate languages SVG
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: top-langs
<<<<<<< HEAD
          options: username=${{ github.repository_owner }}&locale=en&hide_title=false&layout=compact&card_width=420&langs_count=6&theme=dracula&hide_border=false
=======
          options: username=${{ github.repository_owner }}&locale=en&hide_title=false&layout=compact&card_width=420&langs_count=8&theme=dracula&hide_border=false
>>>>>>> f7e7a569008e2387d162ae023a4b409a335e9869
          path: dist/languages.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Push languages SVG to output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: languages-output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
